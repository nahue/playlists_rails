<% content_for :title, "New playlist" %>

<div class="md:w-2/3 w-full">
  <h1 class="font-bold text-4xl">New playlist</h1>

  <%= render "form", playlist: @playlist %>

  <%= link_to "Back to playlists", playlists_path, class: "btn" %>
</div>